ktor {
    deployment {
        port = 8089
        port = ${?PORT}
    }
    application {
        modules = [ com.cinematica.backend.app..ApplicationKt.module ]
    }
    jwt {
        issuer = "http://0.0.0.0:8080"
        domain = "http://0.0.0.0:8080"
        audience = "jwt-audience"
        realm = "ktor jwt auth server"
    }
}